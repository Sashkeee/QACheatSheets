#!/usr/bin/env python3
import requests
import json
import sys

POCKETBASE_URL = "http://5.35.126.199:8090"
COLLECTION = "mini_articles"
ADMIN_TOKEN = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJleHAiOjE3NzQyMTY0MDYsImlkIjoidHRuN3l0anZkYnBva2RpIiwidHlwZSI6ImFkbWluIn0.Y2N-05PJ_QXhgnlqYCtnFss9TwnjhKeIOp9YJP0HQUg"

article = {
    "title": "Docker: Основы контейнеризации",
    "slug": "docker-basics",
    "description": "Введение в Docker. Контейнеризация, образы, контейнеры. Применение в QA и автоматизации тестирования.",
    "content": """Docker — это популярная технология, которая решает классическую проблему IT-индустрии: «На моем компьютере всё работает, а на сервере — сломалось!». Суть контейнеризации заключается в том, что приложение и все необходимые ему для работы зависимости упаковываются в единый изолированный блок, называемый контейнером.

## Зачем Docker нужен в автоматизации и QA?

В современном тестировании контейнеры открывают огромные возможности. Если раньше для проверки работы приложения с базой данных приходилось использовать in-memory базы или писать искусственные заглушки (моки), то теперь можно проводить интеграционные тесты на реальных сервисах.

Вы можете поместить эталонную БД в образ Docker и автоматически создавать экземпляр нового контейнера для каждого теста. При таком подходе база данных запускается в контейнере Docker непосредственно перед каждым тестом, выступая в роли изолированной внепроцессной зависимости.

Кроме того, Docker является базовым стандартом современной ИТ-инфраструктуры. Он отлично работает в связке с системами непрерывной интеграции (например, GitHub Actions) для автоматического развертывания веб-приложений и сервисов.

## Практический пример: Testcontainers

Отличным примером применения Docker в автоматизации тестирования является библиотека **Testcontainers**. Она предоставляет простой API для загрузки реальных сервисов прямо во время выполнения автотестов.

Например, вашему тесту нужна чистая база данных PostgreSQL. В коде автотеста вы просто указываете нужный образ: `postgres:16-alpine`. При запуске система сама обратится к DockerHub, скачает этот образ (если его еще нет локально), запустит контейнер, выполнит тестовые проверки, а после их завершения автоматически остановит контейнер.

## Преимущества для QA

Благодаря этому членам команды больше не нужно устанавливать и настраивать PostgreSQL на своих локальных компьютерах для запуска тестов. Docker гарантирует, что:

- **Окружение консистентно** — одинаково на локальной машине, в CI/CD и на продакшене
- **Нет конфликтов** — каждый контейнер изолирован и не влияет на другие
- **Быстрый старт** — контейнер запускается за секунды
- **Легко масштабировать** — можно запустить множество контейнеров параллельно

Docker — это фундамент современной автоматизированной разработки и тестирования.""",
    "category": "tools"
}

try:
    response = requests.post(
        f"{POCKETBASE_URL}/api/collections/{COLLECTION}/records",
        json=article,
        headers={
            "Content-Type": "application/json",
            "Authorization": f"Bearer {ADMIN_TOKEN}"
        }
    )

    if response.status_code == 201:
        sys.stderr.write("✅ Docker статья опубликована!\n")
        sys.stderr.write(f"ID: {response.json()['id']}\n")
    else:
        sys.stderr.write(f"❌ Ошибка: {response.status_code}\n")
        sys.stderr.write(f"{response.text}\n")
except Exception as e:
    sys.stderr.write(f"❌ Ошибка подключения: {e}\n")

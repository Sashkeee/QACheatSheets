services:
  qa-cheatsheet:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: qa-cheatsheet
    ports:
      - "8082:80"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.2'
          memory: 256M

#!/usr/bin/env python3
import requests
import json
import sys

POCKETBASE_URL = "http://5.35.126.199:8090"
COLLECTION = "mini_articles"
ADMIN_TOKEN = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJleHAiOjE3NzQyMTY0MDYsImlkIjoidHRuN3l0anZkYnBva2RpIiwidHlwZSI6ImFkbWluIn0.Y2N-05PJ_QXhgnlqYCtnFss9TwnjhKeIOp9YJP0HQUg"

article = {
    "title": "CI/CD: Непрерывная интеграция и доставка",
    "slug": "cicd-pipeline",
    "description": "Что такое CI/CD. Непрерывная интеграция и доставка. Примеры конвейеров для автоматизации тестирования.",
    "content": """В современной разработке программного обеспечения скорость и стабильность имеют решающее значение. Конвейеры CI/CD (Continuous Integration / Continuous Delivery) — это фундамент автоматизации, который неразрывно связывает процесс написания кода с его финальным релизом для конечных пользователей.

## Непрерывная интеграция (CI)

Исторически программисты могли писать код неделями, а затем пытались объединить его в один проект, что приводило к масштабным конфликтам — так называемому «аду интеграции». Практика CI решает эту проблему: разработчики отправляют свой код в общий репозиторий (например, Git) несколько раз в день.

Каждое такое изменение автоматически запускает процесс сборки на специальном сервере (например, Jenkins, GitLab CI или GitHub Actions). Сразу после успешной сборки запускаются автоматизированные тесты. Это обеспечивает важнейший принцип гибкой разработки — максимально быструю обратную связь.

### Пример CI

Программист Иван исправил баг и сделал `git push`. Система CI автоматически скачала свежий код и запустила 500 юнит-тестов. Если хотя бы один тест падает, сборка помечается красным цветом (ломается), и Иван мгновенно узнает о проблеме, не допуская дефектный код в стабильную ветку.

## Непрерывная доставка (CD)

CD — это логическое продолжение процесса CI. Если новый код успешно скомпилировался и прошел все начальные тесты, система автоматически развертывает (деплоит) его на тестовое или рабочее окружение.

### Пример CD

После успешной проверки кода Ивана, конвейер автоматически упаковывает приложение (например, в изолированный Docker-контейнер) и отправляет его на тестовый сервер (Staging). Теперь QA-инженер может проверить новый функционал. Если всё отлично, релиз-менеджер нажимает одну кнопку, и этот же проверенный контейнер автоматически отправляется на «боевой» сервер (Production) к реальным клиентам.

## Зачем CI/CD нужен тестировщику?

Для QA-инженера CI/CD — это гарантия работы с актуальной версией продукта. Тестировщику больше не нужно просить разработчика собрать билд вручную.

Более того, QA сам встраивает свои автотесты (например, UI-тесты на Playwright или проверки API) прямо в этот конвейер, чтобы они запускались при каждом обновлении тестового стенда.

## Ключевые компоненты CI/CD конвейера

1. **Trigger** — событие, запускающее конвейер (обычно `git push`)
2. **Build** — сборка приложения (компиляция, инсталляция зависимостей)
3. **Test** — автоматические тесты (юнит, интеграционные, API, UI)
4. **Deploy to Staging** — развертывание на тестовый сервер
5. **QA Verification** — проверка QA-инженером
6. **Deploy to Production** — финальный релиз

CI/CD — это не просто инструмент, это философия разработки, которая делает проекты более надежными, быстрыми и безопасными.""",
    "category": "devops"
}

try:
    response = requests.post(
        f"{POCKETBASE_URL}/api/collections/{COLLECTION}/records",
        json=article,
        headers={
            "Content-Type": "application/json",
            "Authorization": f"Bearer {ADMIN_TOKEN}"
        }
    )

    if response.status_code == 201:
        sys.stderr.write("✅ CI/CD статья опубликована!\n")
        sys.stderr.write(f"ID: {response.json()['id']}\n")
    else:
        sys.stderr.write(f"❌ Ошибка: {response.status_code}\n")
        sys.stderr.write(f"{response.text}\n")
except Exception as e:
    sys.stderr.write(f"❌ Ошибка подключения: {e}\n")
